# Database (update with your database URL)
DATABASE_URL="your-database-connection-string-here"

# JWT Secrets (generate strong, unique secrets)
JWT_SECRET="your-jwt-secret-here"
JWT_ACCESS_TOKEN_EXPIRES_IN="15m"
JWT_REFRESH_TOKEN_EXPIRES_IN="7d"

# Session Secret (generate strong, unique secret)
SESSION_SECRET="your-session-secret-here"

# Environment
NODE_ENV="development"

# NVIDIA NIM API Key (required for AI chat functionality)
# Get your API key from: https://build.nvidia.com/
# IMPORTANT: Never commit this key to version control
NVIDIA_API_KEY="your-nvidia-api-key-here"
